# 参数配置

postgresql:
  url: "bg-postgres"
  port: 5432
  username: postgres
  password: 123456

internet:
  PORT: 8080
minio:
  clientPoint: http://localhost:19090
  serverPoint: http://bg-minio:9000
  accessKey: minioadmin  # 访问密钥（默认 minioadmin）
  secretKey: minioadmin  # 密钥（默认 minioadmin）
  bucket: blog

github:
  client-id: Iv23liAI2h53TjkCBu67
  client-secret: 【必填】OAuth Client Secret（从 git 历史清理，请轮换后回填）
  redirect-uri: http://localhost:18016/api/auth/github/callback

jwt:
  secret: 【必填】JWT 签名密钥（从 git 历史清理，请轮换后回填）
  expiration: 604800

frontend:
  url: http://localhost:3000

op:
  url: http://host.docker.internal
  port: 8016

worker:
  api-url: https://api.lxpavilion.top
  admin-token: 【必填】Worker 管理 Token（从 git 历史清理，请轮换后回填）


